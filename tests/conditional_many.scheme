(let ((x 2))
  (begin
    (if (< x 5)
      (display "x is less than 5\n")
      (display "x is greater or equals 5\n")
    )
    (if (< x 1)
      (display "x is less than 1\n")
      (display "x is greater or equals 1\n")
    )
    (if (< 2 6 8 10)
      (display "list is sorted ascending\n")
      (display "list is not sorted\n")
    )
    (if (< 6 8 2 10)
      (display "list is sorted ascending\n")
      (display "list is not sorted\n")
    )
  )
)
